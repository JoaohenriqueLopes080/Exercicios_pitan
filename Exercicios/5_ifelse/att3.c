#include <stdio.h>

int main() {
    
    int inverso, num;
    
    printf("Digite um valor de numero");
    scanf("%d", &num);
    
    if(num > 0) {
        inverso = num 1/n;
        printf("O inverso do Num é: %f" inverso);       
    }
}